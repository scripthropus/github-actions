name: Hello #ワークフロー名

on: push #イベントのトリガー

jobs: #ジョブの定義
  hello: #ジョブID
    runs-on: ubuntu-latest #ランナー
    steps: #ステップの定義
      - run: echo "Hello, world" #シェルコマンドを実行できる
      - uses: actions/checkout@v4 #アクションの呼び出し